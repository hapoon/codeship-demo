- type: serial
  name: example_parallel_step
  steps:
    - service: codeship_example
      command: printenv
    - service: codeship_example
      command: ls
- name: example_unit_test
  service: codeship_example
  command: make test && ls -l && bash -c "curl -s https://codecov.io/bash"
- name: example_tagged_step
  tag: master
  service: codeship_example
  command: echo "Deploy here!"
